import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    paddingTop: 16,
  },
  title: { fontSize: 22, fontWeight: '700', marginBottom: 12 },
  h: { marginTop: 12, fontWeight: '600' },
});

